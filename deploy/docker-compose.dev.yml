services:
  nginx:
    profiles: ["dev", "web"]
    volumes:
      - ../nginx/conf.d/dev.conf.local:/etc/nginx/conf.d/default.conf:ro

  mariadb:
    profiles: ["dev", "core"]
    ports:
      - "13306:3306"

  redis:
    profiles: ["dev", "core"]
    ports:
      - "16379:6379"
